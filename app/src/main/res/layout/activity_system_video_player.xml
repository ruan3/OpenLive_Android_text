<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:gravity="center"
    android:background="#000000"
    android:layout_height="match_parent">

    <io.agora.contract.view.SystemVideoView
        android:id="@+id/sy_video_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        />

    <include layout="@layout/media_controller" android:id="@+id/media_controller"></include>

    <include layout="@layout/ll_buffer" android:id="@+id/ll_buffer"></include>

    <include layout="@layout/ll_loading" android:id="@+id/ll_loading"></include>

</RelativeLayout>